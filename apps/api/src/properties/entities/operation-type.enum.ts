/**
 * Tipo de operacion de una propiedad (property-catalog spec, Requirement:
 * "Especificaciones inherentes a la propiedad" - "Tipo de operacion limitado
 * a venta o alquiler").
 */
export enum OperationType {
  SALE = 'sale',
  RENT = 'rent',
}
