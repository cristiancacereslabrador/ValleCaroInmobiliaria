## 1. Backend - datos de zona (si hiciera falta)

- [x] 1.1 Comprobar si `city`/`postalCode` ya existen en la entidad `Property` (por `neighborhood-market-insights`); si no existen, añadirlos con su migración, y verificar que la migración se aplica correctamente sin duplicar columnas ni afectar datos existentes — YA EXISTEN (`apps/api/src/properties/entities/property.entity.ts` líneas 115-119, columnas `city`/`postal_code`, nullable), añadidas por `neighborhood-market-insights`. No se toca la entidad ni se crea migración nueva.

## 2. Backend - cálculo de estimación

- [x] 2.1 Implementar la selección de comparables (tipo, zona, superficie ±25%, habitaciones ±1, separados por operación venta/alquiler) y verificar con tests unitarios los escenarios de comparables suficientes/insuficientes — `property-valuation-estimator.service.ts` (`findComparablePricesPerM2`/`estimateForOperation`), tests en `property-valuation-estimator.service.spec.ts` (13 tests, incluye umbral por defecto/configurable, venta/alquiler independientes, filtros de zona/superficie/habitaciones)
- [x] 2.2 Implementar el cálculo de percentiles 25-75 de precio/m² sobre los comparables y su aplicación a la superficie indicada, y verificar con tests unitarios el cálculo con datos de ejemplo — `valuation-price-range.util.ts` (función pura `computePercentile`/`computeValuationPriceRange`), tests en `valuation-price-range.util.spec.ts` (9 tests)
- [x] 2.3 Exponer el endpoint de tasación validando los datos obligatorios (tipo, zona, superficie), y verificar con tests los escenarios de `specs/property-valuation-estimator/spec.md` — `POST /api/v1/property-valuation-estimator/estimate` (`property-valuation-estimator.controller.ts`), tests de integración en `property-valuation-estimator.controller.spec.ts` (7 tests: 200 con resultado, 400 por tipo/superficie/zona ausentes, tipo no soportado, campos opcionales)

## 3. Frontend

- [x] 3.1 Implementar el formulario de tasación (independiente del listado de propiedades) con sus campos y validación, y verificar manualmente que envía correctamente los datos — `apps/web/src/components/ValuationEstimatorForm.tsx` + ruta propia `apps/web/src/app/valuation/page.tsx` (no reutiliza `PropertyForm`, no persiste nada). Verificado: `tsc --noEmit` limpio, la ruta `/valuation` compila y devuelve 200 en el `next dev` compartido, y el bundle compilado confirma la llamada real a `POST /property-valuation-estimator/estimate` con el payload del formulario.
- [x] 3.2 Mostrar el resultado (rango de venta y/o alquiler, o el aviso de datos insuficientes) con el aviso de estimación orientativa, y verificar manualmente ambos casos — componente `ValuationResult`/`ValuationRangeCard` en el mismo archivo, cubre `available:true` (rango + nº comparables) y `available:false` (aviso de datos insuficientes) para venta y alquiler por separado, más el `disclaimer` orientativo siempre visible. Verificado por tipado + inspección del bundle compilado (sin navegador disponible en este entorno, mismo método que el resto del proyecto); la forma de los datos ya se confirmó real en el backend (tasks 2.3/4.1).

## 4. Verificación end-to-end

- [x] 4.1 Crear un conjunto de propiedades comparables de prueba en una zona y tipo concretos, solicitar una tasación con esas características y verificar que el rango devuelto es coherente con los datos cargados, según los escenarios de la spec — verificado por curl contra la API real (`nest start --watch`, MariaDB real): 3 pisos en venta en "ValuationTestCity" (100 m², 3 hab., precios 210000/220000/230000 → precio/m² 2100/2200/2300) devuelven `sale: {available:true, minPrice:215000, maxPrice:225000, comparablesCount:3}` (percentiles 25/75 correctos a mano) y `rent: {available:false, reason:'insufficient-data'}` (sin comparables de alquiler); normalización de ciudad (espacios/mayúsculas) igual que price-trends; filtro de habitaciones ±1 excluye correctamente (bedrooms=5 vs comparables con 3 → insuficiente); filtro de superficie ±25% excluye correctamente (surfaceM2=300 vs comparables de 100 → insuficiente). Datos de prueba eliminados al terminar (204 en el DELETE, y la re-consulta vuelve a `insufficient-data`).
